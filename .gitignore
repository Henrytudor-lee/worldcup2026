# ============ 1_数据基础 整个目录保护 ============
# 1248 球员主表是项目核心资产，**不进 git**（避免仓库膨胀 + 隐私）
# 已由 SPA 内嵌 JSON / 后端直接读取部署
1_数据基础/

# ============ 备份文件 ============
*.bak
*.bakDATE
*.bak20*
_backup/
backups/
_archive_v1/
_archive/

# ============ Python ============
*.pyc
__pycache__/
.mavis/cache/

# ============ macOS ============
.DS_Store

# ============ 编辑器临时 ============
*.swp
*.swo
*~
.vscode/
.idea/

# ============ 4_比赛预测 子仓库（避免嵌套 git 仓库）===========
# 4_比赛预测/ 现在只是个普通目录（git 仓库已合并到根目录）
# 但里面还残留 .git/ 目录，必须排除
4_比赛预测/.git/

# ============ Mavis 内部缓存 ============
.mavis/

# ============ 大文件 / 日志 ============
*.log
*.tmp
nohup.out
