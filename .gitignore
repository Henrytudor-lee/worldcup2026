# ============ 1_数据基础 4 个核心 CSV 进 git ============
# 1248 球员主表 + 教练 + FIFA + 赛程：让 clone 即跑
# 但 ranking 派生 CSV + backup 不进 git
1_数据基础/world_cup_2026_ranking_v2_0.csv
1_数据基础/*.bak
1_数据基础/*.bak*

# ============ 备份文件 ============
*.bak
*.bakDATE
*.bak20*
_backup/
backups/
_archive_v1/
_archive/

# ============ Python ============
*.pyc
__pycache__/
.mavis/cache/

# ============ Python venv (含 skopt) ============
backend/.venv/

# ============ macOS ============
.DS_Store

# ============ 编辑器临时 ============
*.swp
*.swo
*~
.vscode/
.idea/

# ============ 4_比赛预测 子仓库（避免嵌套 git 仓库）===========
# 4_比赛预测/ 现在只是个普通目录（git 仓库已合并到根目录）
# 但里面还残留 .git/ 目录，必须排除
4_比赛预测/.git/

# ============ Mavis 内部缓存 ============
.mavis/

# ============ 运行时临时文件 ============
.run/

# ============ 大文件 / 日志 ============
*.log
*.tmp
nohup.out
backend/.venv/

# ============ 临时调试文件 ============
.playwright-mcp/
spa-*.png
fbref_*.json
fbref_all_teams.json
fbref_data.json
fbref_scraped.json
scrape_fbref*.py
arsenal_stats_snapshot.txt

# ============ 临时脚本 (另一 agent 跑的 age 补全等) ============
1_数据基础/age*.py
1_数据基础/fill_age*.py
1_数据基础/add_age*.py

# ============ ESPN 抓取产物 (raw 可重抓) ============
1_数据基础/espn_match_data/
1_数据基础/espn_cron_report.json

# ============ 校准已删, 产物/报告 不进 git ============
5_算法/calibration_*.json
4_可视化/
