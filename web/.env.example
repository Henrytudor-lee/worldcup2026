# Next.js 16 SPA 数据源配置

# static = 读 public/static-data/*.json (无后端, 离线可跑)
# live   = 调 NEXT_PUBLIC_BACKEND_URL (FastAPI 8766)
NEXT_PUBLIC_DATA_SOURCE=static

# FastAPI 后端 (仅当 NEXT_PUBLIC_DATA_SOURCE=live 时生效)
NEXT_PUBLIC_BACKEND_URL=http://localhost:8766
